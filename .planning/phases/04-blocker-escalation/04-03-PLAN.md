---
phase: 04-blocker-escalation
plan: 03
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - internal/session/run_rebuild.go
  - internal/session/run_rebuild_test.go
autonomous: true
requirements:
  - BLOCK-02
  - BLOCK-03
must_haves:
  truths:
    - Operators can inspect blocker status, current owner, reason, next action, and recommended action directly under the source task in `run show`.
    - Blocker visibility is task-local only; no new global blocker dashboard or list command appears in this phase.
    - Broken blocker/source/reroute links fail loudly during rebuild instead of rendering misleading partial blocker chains.
  artifacts:
    - path: internal/session/run_rebuild.go
      provides: blocker artifact loading, linkage validation, and task-local `run show` blocker rendering
    - path: internal/session/run_rebuild_test.go
      provides: blocker render coverage and blocker-link drift rejection tests
  key_links:
    - from: internal/session/run_rebuild.go
      to: '`coordinator/runs/<run-id>/blockers/*.yaml`'
      via: blocker-case loading and source-task attachment
      pattern: 'loadRunBlocker|Blocker:'
    - from: internal/session/run_rebuild.go
      to: internal/protocol/coordinator.go
      via: `RunGraphTask.BlockerCase` and blocker status/kind rendering
      pattern: 'BlockerCase'
---

<objective>
Extend `tmuxicate run show <run-id>` so blocker escalation is visible from durable artifacts on the same task-local inspection surface as review handoffs, with mismatch detection instead of silent drift.

Purpose: Deliver the operator-facing half of BLOCK-02 and BLOCK-03 without pulling Phase 5 summary work forward.
Output: blocker-case load/validation in `LoadRunGraph`, blocker rendering in `FormatRunGraph`, and rebuild tests for task-local blocker visibility plus broken-link rejection.
</objective>

<execution_context>
@/Users/chsong/.codex/get-shit-done/workflows/execute-plan.md
@/Users/chsong/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-blocker-escalation/04-CONTEXT.md
@.planning/phases/04-blocker-escalation/04-RESEARCH.md
@.planning/phases/04-blocker-escalation/04-VALIDATION.md
@.planning/phases/03-review-handoff-flow/03-02-SUMMARY.md
@.planning/codebase/TESTING.md
@internal/session/run_rebuild.go
@internal/session/run_rebuild_test.go
@internal/session/task_cmd.go
@internal/protocol/coordinator.go
@internal/mailbox/coordinator_store.go

<interfaces>
Expected from Plan `04-01`:
```go
type BlockerCase struct {
	RunID             RunID
	SourceTaskID      TaskID
	SourceMessageID   MessageID
	CurrentTaskID     TaskID
	CurrentMessageID  MessageID
	CurrentOwner      AgentName
	DeclaredState     string
	WaitKind          WaitKind
	BlockKind         BlockKind
	Reason            string
	SelectedAction    BlockerAction
	Status            BlockerStatus
	RerouteCount      int
	MaxReroutes       int
	RecommendedAction *RecommendedAction
	Resolution        *BlockerResolution
}
```

Existing run-show seam to extend:
```go
type RunGraphTask struct {
	Task          protocol.ChildTask
	ReceiptState  protocol.FolderState
	DeclaredState string
	ReviewHandoff *protocol.ReviewHandoff
}

func LoadRunGraph(stateDir string, runID protocol.RunID) (*RunGraph, error)
func FormatRunGraph(graph *RunGraph) string
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add red rebuild tests for task-local blocker blocks and broken-link rejection</name>
  <files>
internal/session/run_rebuild_test.go
  </files>
  <read_first>
.planning/phases/04-blocker-escalation/04-CONTEXT.md
.planning/phases/04-blocker-escalation/04-RESEARCH.md
internal/session/run_rebuild.go
internal/session/run_rebuild_test.go
internal/session/task_cmd_test.go
  </read_first>
  <behavior>
    - `run show` should render blocker state under the source task, not in a new top-level blockers section, per D-16 and D-17.
    - The derived blocker block should coexist with the existing review-handoff block when both workflows apply to the same source task.
    - Drift in blocker source/current task or message linkage should fail loudly, matching the review-handoff rebuild discipline.
  </behavior>
  <acceptance_criteria>
    - `rg -n "TestRunShowIncludesTaskLocalBlockerBlock|TestRunShowIncludesBlockerAndReviewBlocksTogether|TestLoadRunGraphRejectsBrokenBlockerLinks" internal/session/run_rebuild_test.go` returns three matches.
    - `go test ./internal/session -run 'TestRunShowIncludesTaskLocalBlockerBlock|TestRunShowIncludesBlockerAndReviewBlocksTogether|TestLoadRunGraphRejectsBrokenBlockerLinks' -count=1` runs and is expected to fail until Task 2 adds blocker rebuild support.
  </acceptance_criteria>
  <action>
Extend `internal/session/run_rebuild_test.go` with red coverage for blocker visibility and linkage validation.

Add three exact tests:
- `TestRunShowIncludesTaskLocalBlockerBlock`
- `TestRunShowIncludesBlockerAndReviewBlocksTogether`
- `TestLoadRunGraphRejectsBrokenBlockerLinks`

Fixture requirements:
- seed a source implementation task with a blocker case under `coordinator/runs/<run-id>/blockers/<source-task-id>.yaml`
- include `status=escalated`, `current_owner`, `reason`, `selected_action`, `reroute_count`, `max_reroutes`, and `recommended_action.kind`
- for the mixed-workflow test, also seed an existing review handoff so the output proves both derived blocks can appear under the same source task without a separate blocker summary section

Assertions must pin exact output snippets such as:
- `Blocker: escalated`
- `Current Owner:`
- `Next Action:`
- `Reroutes:`
- `Recommended Action:`

and must additionally assert that the formatted output does not introduce a top-level `Blockers:` header.
  </action>
  <verify>
    <automated>go test ./internal/session -run 'TestRunShowIncludesTaskLocalBlockerBlock|TestRunShowIncludesBlockerAndReviewBlocksTogether|TestLoadRunGraphRejectsBrokenBlockerLinks' -count=1</automated>
  </verify>
  <done>
Red tests exist for blocker rendering, mixed blocker+review rendering, and blocker artifact drift rejection, with explicit output and mismatch expectations.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Load, validate, and render blocker cases under the source task in `run show`</name>
  <files>
internal/session/run_rebuild.go
internal/session/run_rebuild_test.go
  </files>
  <read_first>
.planning/phases/04-blocker-escalation/04-CONTEXT.md
.planning/phases/04-blocker-escalation/04-RESEARCH.md
internal/session/run_rebuild.go
internal/session/run_rebuild_test.go
internal/protocol/coordinator.go
internal/mailbox/coordinator_store.go
  </read_first>
  <behavior>
    - Blocker visibility must be derived entirely from durable artifacts, not from the agent-global `state.current.json` snapshot.
    - The source task remains the anchor; rerouted current tasks are referenced from the blocker block instead of becoming a second blocker summary row.
    - Rebuild must reject broken blocker links instead of rendering partial or misleading blocker state.
  </behavior>
  <acceptance_criteria>
    - `rg -n "BlockerCase \\*protocol.BlockerCase|Blocker: |Current Task: |Current Owner: |Declared State: |Wait Kind: |Block Kind: |Reason: |Next Action: |Reroutes: |Recommended Action: |Resolution:" internal/session/run_rebuild.go` returns matches.
    - `rg -n "loadRunBlocker|RunBlockersDir|ReadBlockerCase" internal/session/run_rebuild.go` returns matches.
    - `go test ./internal/session -run 'TestRunShowIncludesTaskLocalBlockerBlock|TestRunShowIncludesBlockerAndReviewBlocksTogether|TestLoadRunGraphRejectsBrokenBlockerLinks' -count=1` passes.
    - `go test ./internal/session -run 'TestRunShowIncludesReviewHandoffBlock|TestLoadRunGraphRejectsBrokenReviewHandoffLinks' -count=1` still passes.
  </acceptance_criteria>
  <action>
Extend `internal/session/run_rebuild.go` with blocker-case support.

Implementation requirements:
- add `BlockerCase *protocol.BlockerCase` to `RunGraphTask`
- load blocker cases from `coordinator/runs/<run-id>/blockers/*.yaml`
- attach each blocker case to the source task only; do not create a separate task row or a run-level blocker section

Validation rules to enforce during `LoadRunGraph`:
- `blocker.run_id == run.RunID`
- `source_task_id` must exist in the run task set
- `source_message_id` must match the source task's message id
- when `current_task_id` is present, it must exist in the run task set
- when `current_message_id` is present, it must match the current task's message id
- when `current_owner` is present, it must match the current task's owner
- any `resolution.created_task_id` or `resolution.created_message_id` must match real run artifacts if present
- mismatches must return `coordinator artifact mismatch`, mirroring the existing review-handoff failure mode

Render the blocker block directly under the source task with these exact labels when data is present:
- `Blocker:`
- `Current Task:`
- `Current Owner:`
- `Current Message:`
- `Declared State:`
- `Wait Kind:`
- `Block Kind:`
- `Reason:`
- `Next Action:`
- `Reroutes:`
- `Recommended Action:`
- `Resolution:`

Do not add a `tmuxicate blocker show` command, a `Blockers:` top-level header, aggregate counts, or any Phase 5 summary behavior in this plan.
  </action>
  <verify>
    <automated>go test ./internal/session -run 'TestRunShowIncludesTaskLocalBlockerBlock|TestRunShowIncludesBlockerAndReviewBlocksTogether|TestLoadRunGraphRejectsBrokenBlockerLinks|TestRunShowIncludesReviewHandoffBlock|TestLoadRunGraphRejectsBrokenReviewHandoffLinks' -count=1 && go test ./internal/session -count=1</automated>
  </verify>
  <done>
`run show` now rebuilds blocker chains from `blockers/*.yaml`, renders the blocker block directly under the source task, and fails loudly on blocker/source/current-task drift without introducing any run-level blocker summary UX.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Durable blocker YAML -> run-show rebuild | `run show` trusts blocker artifacts only after explicit linkage validation. |
| Mixed workflow artifacts -> operator output | Review handoffs and blocker cases can both annotate one source task and must not cross-wire or hide each other. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | T | `internal/session/run_rebuild.go` | mitigate | Validate blocker `run_id`, `source_task_id`, `source_message_id`, and current task/message/owner links before attaching a blocker block. |
| T-04-03-02 | I | `internal/session/run_rebuild.go` | mitigate | Keep blocker visibility task-local under the source task and avoid introducing a new top-level blocker section that could misattribute unrelated state. |
| T-04-03-03 | R | `internal/session/run_rebuild.go` | mitigate | Surface `coordinator artifact mismatch` on broken blocker links so operators can see when durable lineage is untrustworthy instead of consuming partial output. |
</threat_model>

<verification>
- `go test ./internal/session -run 'TestRunShowIncludesTaskLocalBlockerBlock|TestRunShowIncludesBlockerAndReviewBlocksTogether|TestLoadRunGraphRejectsBrokenBlockerLinks|TestRunShowIncludesReviewHandoffBlock|TestLoadRunGraphRejectsBrokenReviewHandoffLinks' -count=1`
- `go test ./internal/session -count=1`
</verification>

<success_criteria>
- `LoadRunGraph` can rebuild blocker cases from durable artifacts and attach them to the correct source task only.
- `FormatRunGraph` renders blocker details directly under the source task with the specified scan-friendly labels.
- Blocker drift is rejected with the same fail-loud behavior already used for review handoff mismatches, and no Phase 5 blocker-summary UX is introduced early.
</success_criteria>

<output>
After completion, create `.planning/phases/04-blocker-escalation/04-03-SUMMARY.md`
</output>
