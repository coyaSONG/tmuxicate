---
phase: 09-run-timeline-views
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - cmd/tmuxicate/main.go
  - cmd/tmuxicate/main_test.go
  - internal/session/run_rebuild.go
  - internal/session/run_rebuild_test.go
autonomous: true
requirements:
  - OBS-01
  - OBS-02
must_haves:
  truths:
    - "Operators can inspect a compact per-run timeline from existing `tmuxicate run show` workflows without transcript spelunking."
    - "Operators can filter timeline output by owner, state, task class, and execution target using explicit CLI flags tied to durable metadata."
    - "Timeline display remains additive to the current run summary and task-detail surfaces, with an explicit timeline-only mode rather than a new reporting subsystem."
  artifacts:
    - path: cmd/tmuxicate/main.go
      provides: "`run show` timeline flags and filter parsing on the existing Cobra command surface"
    - path: internal/session/run_rebuild.go
      provides: "Additive run-show rendering that inserts filtered timeline output without replacing the current summary/task detail blocks"
    - path: cmd/tmuxicate/main_test.go
      provides: "CLI regression coverage for timeline flags, filtered output, and timeline-only behavior"
    - path: internal/session/run_rebuild_test.go
      provides: "Formatter coverage for timeline placement, filter application, and backward compatibility when timeline flags are absent"
  key_links:
    - from: cmd/tmuxicate/main.go
      to: internal/session/run_rebuild.go
      via: "`run show` flag parsing must feed one run-view rendering path so timeline and task-detail output cannot drift"
      pattern: "run show|timeline|timeline-only|timeline-owner|timeline-state|timeline-class|timeline-target"
    - from: internal/session/run_rebuild.go
      to: internal/session/run_timeline.go
      via: "RunGraph rendering consumes the Plan 09-01 projection/filter layer instead of rebuilding a second event model"
      pattern: "BuildRunTimeline|FilterRunTimeline|RunTimeline"
---

<objective>
Expose the run timeline through existing operator run-inspection workflows with explicit, durable filters.

Purpose: Complete OBS-01 and OBS-02 by wiring the Phase 09 timeline read model into `tmuxicate run show` so operators can inspect chronological flow and narrow it by owner, state, task class, or execution target without reading transcripts.
Output: Timeline-aware `run show` flags, additive rendering between summary and task detail blocks, and regression tests that pin compact filtered terminal output.
</objective>

<execution_context>
@/Users/chsong/.codex/get-shit-done/workflows/execute-plan.md
@/Users/chsong/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-run-timeline-views/09-CONTEXT.md
@.planning/phases/09-run-timeline-views/09-01-PLAN.md
@.planning/phases/07-partial-replanning-flow/07-02-SUMMARY.md
@.planning/phases/06-adaptive-routing-signals/06-02-SUMMARY.md
@.planning/milestones/v1.0-phases/05-run-summaries/05-02-SUMMARY.md
@cmd/tmuxicate/main.go
@internal/session/run_rebuild.go
@internal/session/log_view.go

<interfaces>
Expected from Plan 09-01:
```go
type RunTimeline struct {
    RunID  protocol.RunID
    Events []RunTimelineEvent
}

type RunTimelineEvent struct {
    Timestamp       time.Time
    Kind            string
    Owner           protocol.AgentName
    State           string
    TaskClass       protocol.TaskClass
    ExecutionTarget string
    TaskID          protocol.TaskID
    MessageID       protocol.MessageID
    Summary         string
}

type RunTimelineFilter struct {
    Owner           string
    State           string
    TaskClass       protocol.TaskClass
    ExecutionTarget string
}

func BuildRunTimeline(stateDir string, graph *RunGraph) (*RunTimeline, error)
func FilterRunTimeline(timeline *RunTimeline, filter RunTimelineFilter) []RunTimelineEvent
```

Current run-show seam:
```go
func newRunShowCmd() *cobra.Command
func FormatRunGraph(graph *RunGraph) string
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add red tests for timeline-aware `run show` output and explicit filters</name>
  <files>cmd/tmuxicate/main_test.go, internal/session/run_rebuild_test.go</files>
  <behavior>
    - Per D-04, tests must keep timeline output inside existing `run show` workflows instead of introducing a second reporting command.
    - Per D-05, tests must pin explicit owner, state, task class, and execution-target filters on top of the Plan 09-01 read model.
    - Per D-06, tests must keep the timeline compact and additive: summary first, timeline second, task detail blocks after that unless timeline-only mode is requested.
  </behavior>
  <action>
Extend the existing Cobra and run-rebuild test suites with these exact tests:
- `TestRunShowCommandPrintsTimelineSectionWithExplicitFilters`
- `TestRunShowCommandTimelineOnlyOmitsTaskBlocks`
- `TestFormatRunGraphIncludesTimelineBetweenSummaryAndTaskDetails`
- `TestFormatRunGraphFiltersTimelineByOwnerStateClassAndExecutionTarget`

Pin this CLI shape exactly:
- `tmuxicate run show <run-id> --timeline`
- `--timeline-only`
- `--timeline-owner`
- `--timeline-state`
- `--timeline-class`
- `--timeline-target`

Test expectations:
- timeline output uses a `Timeline:` section header
- when any filter is active, output also includes a compact `Timeline Filters:` line summarizing the active exact-match filters
- filtered timeline rows include enough stable tokens to prove owner/state/class/target filtering worked without asserting an over-specific full-line template
- default `run show` behavior stays backward-compatible when no timeline flags are passed
- `--timeline-only` preserves the run header and shared summary but omits per-task detail blocks
  </action>
  <verify>
    <automated>go test ./cmd/tmuxicate ./internal/session -run 'TestRunShowCommandPrintsTimelineSectionWithExplicitFilters|TestRunShowCommandTimelineOnlyOmitsTaskBlocks|TestFormatRunGraphIncludesTimelineBetweenSummaryAndTaskDetails|TestFormatRunGraphFiltersTimelineByOwnerStateClassAndExecutionTarget' -count=1</automated>
  </verify>
  <done>Red tests pin the operator-facing timeline CLI contract, additive placement, explicit filter behavior, and timeline-only mode before rendering code changes land.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire filtered timeline rendering into the existing `run show` workflow</name>
  <files>cmd/tmuxicate/main.go, cmd/tmuxicate/main_test.go, internal/session/run_rebuild.go, internal/session/run_rebuild_test.go</files>
  <action>
Implement timeline-aware run inspection by consuming the Plan 09-01 projection instead of creating a new reporting path.

CLI wiring:
- extend `newRunShowCmd` with `--timeline`, `--timeline-only`, `--timeline-owner`, `--timeline-state`, `--timeline-class`, and `--timeline-target`
- treat any filter flag as implying timeline mode so operators do not need redundant switches
- validate timeline-only/filter combinations at the Cobra boundary and keep non-timeline `run show` behavior unchanged

Rendering rules:
- Per D-04, keep `run show` as the canonical inspection surface
- Per D-05, build one `RunTimelineFilter` from CLI flags and apply it to the Plan 09-01 timeline projection
- Per D-06, render the timeline after the shared summary block and before per-task detail blocks
- when `--timeline-only` is set, show run header + summary + filtered timeline and suppress task-local blocks
- keep timeline lines compact and terminal-readable; include timestamp, event kind, and whichever of owner/state/class/target/task refs are available

Durability rules:
- Per D-01 and D-02, render only from the derived timeline projection over current durable artifacts/state events
- Per D-03, bubble projection errors up rather than dropping bad events
- Per D-07, filter execution targets using persisted placement metadata from Phase 8 assumptions, not current tmux pane state

Do not redesign `run show` into a new command family and do not add transcript-derived event extraction.
  </action>
  <verify>
    <automated>go test ./cmd/tmuxicate ./internal/session -run 'TestRunShowCommandPrintsTimelineSectionWithExplicitFilters|TestRunShowCommandTimelineOnlyOmitsTaskBlocks|TestFormatRunGraphIncludesTimelineBetweenSummaryAndTaskDetails|TestFormatRunGraphFiltersTimelineByOwnerStateClassAndExecutionTarget|TestRunShowCommandPrintsSummaryUnderHeader' -count=1</automated>
  </verify>
  <done>`tmuxicate run show` can now render filtered timeline views from durable run history while preserving existing summary and task-detail workflows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI flags -> timeline filter | Operator-provided filters shape which durable events are shown |
| run timeline projection -> terminal output | Compact display depends on correct propagation of durable owner/state/class/target fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | T | `run show` filter parsing | mitigate | Parse explicit timeline flags into one typed filter struct and reject/normalize invalid combinations before rendering |
| T-09-05 | I | timeline output | mitigate | Render owner/state/class/target strictly from projected durable fields, never from tmux/transcript inference |
| T-09-06 | D | operator terminal output | mitigate | Keep timeline additive and support `--timeline-only` so large runs do not force noisy mixed views |
</threat_model>

<verification>
- `go test ./cmd/tmuxicate ./internal/session -run 'TestRunShowCommandPrintsTimelineSectionWithExplicitFilters|TestRunShowCommandTimelineOnlyOmitsTaskBlocks|TestFormatRunGraphIncludesTimelineBetweenSummaryAndTaskDetails|TestFormatRunGraphFiltersTimelineByOwnerStateClassAndExecutionTarget|TestRunShowCommandPrintsSummaryUnderHeader' -count=1`
- `go test ./cmd/tmuxicate ./internal/session -count=1`
</verification>

<success_criteria>
- `run show` exposes timeline mode and exact-match filters for owner, state, task class, and execution target.
- Timeline output is rendered from the Phase 09 read model and inserted between the shared summary and task detail blocks.
- Timeline-only mode preserves run header and summary while suppressing per-task detail noise.
- Existing `run show` output without timeline flags remains backward-compatible.
</success_criteria>

<output>
After completion, create `.planning/phases/09-run-timeline-views/09-02-SUMMARY.md`
</output>
