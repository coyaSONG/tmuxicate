---
phase: 09-run-timeline-views
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/session/run_timeline.go
  - internal/session/run_timeline_test.go
autonomous: true
requirements:
  - OBS-01
must_haves:
  truths:
    - "A coordinator run can be rebuilt into one chronological timeline covering task creation, routing, review, blocker, partial replan, resolution, and agent state transitions."
    - "Timeline events carry durable owner, state, task class, and execution-target fields so later operator filters do not need transcript review or tmux inspection."
    - "Timeline rebuild fails loudly when durable artifacts and state-event records disagree about message identity, ownership, or lineage."
  artifacts:
    - path: internal/session/run_timeline.go
      provides: "Pure run-timeline projection, deterministic sort/filter rules, and exported read-model contracts over RunGraph plus state-event files"
    - path: internal/session/run_timeline_test.go
      provides: "Regression coverage for event projection, stable ordering, execution-target filter fields, and fail-loud rebuild behavior"
  key_links:
    - from: internal/session/run_rebuild.go
      to: internal/session/run_timeline.go
      via: "RunGraph remains the canonical artifact validation layer before any timeline event projection happens"
      pattern: "LoadRunGraph|RunGraph|RunGraphTask"
    - from: internal/session/task_cmd.go
      to: internal/session/run_timeline.go
      via: "TaskEvent rows in `state.jsonl` are matched back to canonical task/message ownership for lifecycle events"
      pattern: "TaskEvent|task.accept|task.wait|task.block|task.done|review.respond"
    - from: .planning/phases/08-remote-execution-targets/08-01-PLAN.md
      to: internal/session/run_timeline.go
      via: "Phase 8 placement metadata is copied onto projected timeline events for execution-target filters"
      pattern: "Placement|ExecutionTarget|TaskPlacement"
---

<objective>
Build the run-timeline read model as a strict projection over existing coordinator artifacts and per-agent state events.

Purpose: Satisfy the read-model half of OBS-01 by deriving a deterministic, testable timeline from durable YAML and JSONL inputs only, with no new persisted timeline store or transcript mining.
Output: Exported timeline contracts plus red/green test coverage for chronological event projection, filter-ready metadata, and fail-loud mismatch handling.
</objective>

<execution_context>
@/Users/chsong/.codex/get-shit-done/workflows/execute-plan.md
@/Users/chsong/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-run-timeline-views/09-CONTEXT.md
@.planning/phases/08-remote-execution-targets/08-01-PLAN.md
@.planning/phases/08-remote-execution-targets/08-02-PLAN.md
@.planning/phases/07-partial-replanning-flow/07-02-SUMMARY.md
@.planning/phases/06-adaptive-routing-signals/06-02-SUMMARY.md
@.planning/milestones/v1.0-phases/05-run-summaries/05-01-SUMMARY.md
@internal/session/task_cmd.go
@internal/session/review_response.go
@internal/session/run.go
@internal/session/run_rebuild.go
@internal/session/run_summary.go

<interfaces>
Existing durable event seam:
```go
type TaskEvent struct {
    Schema        string               `json:"schema"`
    Timestamp     string               `json:"ts"`
    Agent         string               `json:"agent"`
    Event         string               `json:"event"`
    DeclaredState string               `json:"declared_state"`
    MessageID     protocol.MessageID   `json:"message_id"`
    Thread        protocol.ThreadID    `json:"thread"`
    ReceiptState  protocol.FolderState `json:"receipt_state"`
    WaitingOn     string               `json:"waiting_on,omitempty"`
    BlockedOn     string               `json:"blocked_on,omitempty"`
    Reason        string               `json:"reason,omitempty"`
    Summary       string               `json:"summary,omitempty"`
}
```

Existing canonical run rebuild seam:
```go
type RunGraph struct {
    Run   protocol.CoordinatorRun
    Tasks []RunGraphTask
}

type RunGraphTask struct {
    Task          protocol.ChildTask
    ReceiptState  protocol.FolderState
    DeclaredState string
    ReviewHandoff *protocol.ReviewHandoff
    BlockerCase   *protocol.BlockerCase
    PartialReplan *protocol.PartialReplan
    ReplanSource  protocol.TaskID
    Supersedes    protocol.TaskID
}

func LoadRunGraph(stateDir string, runID protocol.RunID) (*RunGraph, error)
```

Assumed Phase 8 placement seam:
```go
type TaskPlacement struct {
    Target ExecutionTarget `yaml:"target"`
    Reason string          `yaml:"reason"`
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add red tests for strict run-timeline projection and deterministic filtering</name>
  <files>internal/session/run_timeline_test.go</files>
  <behavior>
    - Per D-01 and D-02, tests must prove the timeline is rebuilt from durable coordinator artifacts plus per-agent `state.jsonl`, not transcripts or a new persisted store.
    - Per D-03 and D-08, tests must force fail-loud errors when a matching state-event row disagrees with canonical run/task ownership or message linkage.
    - Per D-05 and D-07, tests must prove owner, state, task class, and execution-target filters operate on durable metadata, including Phase 8 placement fields.
  </behavior>
  <action>
Create `internal/session/run_timeline_test.go` in package `session` and reuse existing run-graph fixture helpers from `internal/session/run_rebuild_test.go` instead of inventing a second fixture system.

Add these exact tests:
- `TestBuildRunTimelineIncludesRoutingReviewBlockerReplanAndStateTransitions`
- `TestBuildRunTimelineSortsDeterministicallyWhenTimestampsCollide`
- `TestBuildRunTimelineRejectsStateEventAgentMismatch`
- `TestFilterRunTimelineByOwnerStateClassAndExecutionTarget`

Cover these expectations exactly:
- timeline output contains durable events for run creation, child-task creation/routing, review handoff progression, blocker escalation/resolution, partial-replan linkage, and task lifecycle transitions sourced from `TaskEvent`
- event ordering is stable on timestamp collisions by applying a deterministic secondary key instead of map iteration order
- a `TaskEvent` row for a known run task must error when its `Agent`, `MessageID`, or `Thread` disagrees with the canonical task/message ownership for that run
- execution-target filtering uses Phase 8 placement metadata attached to tasks, not owner-name heuristics; mutate fixture task YAML to include at least one non-local target and one implicit local target
- empty filters return all projected events, while exact-match filters narrow the set predictably without transcript access
  </action>
  <verify>
    <automated>go test ./internal/session -run 'TestBuildRunTimelineIncludesRoutingReviewBlockerReplanAndStateTransitions|TestBuildRunTimelineSortsDeterministicallyWhenTimestampsCollide|TestBuildRunTimelineRejectsStateEventAgentMismatch|TestFilterRunTimelineByOwnerStateClassAndExecutionTarget' -count=1</automated>
  </verify>
  <done>Red tests pin the Phase 9 timeline source-of-truth, ordering, mismatch rejection, and filter metadata before any projection code lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the pure run-timeline projection and filter contracts</name>
  <files>internal/session/run_timeline.go, internal/session/run_timeline_test.go</files>
  <action>
Implement `internal/session/run_timeline.go` as a pure read-model module that depends on existing durable state only.

Create exported contracts sized for downstream run-view integration:
- `RunTimeline`
- `RunTimelineEvent`
- `RunTimelineFilter`
- `BuildRunTimeline(...)`
- one pure filter helper such as `FilterRunTimeline(...)`

Implementation rules:
- Per D-01 and D-02, derive events only from `RunGraph`, review/blocker/partial-replan artifacts already validated by `LoadRunGraph`, and per-agent `state.jsonl` rows
- Per D-03, do not silently guess through artifact drift; return `coordinator artifact mismatch`-style errors when matched state-event ownership, thread, or message references are inconsistent
- Per D-05, copy durable owner, effective state, task class, and execution-target fields onto each timeline event so filtering is data-driven and exact-match
- Per D-07, consume task placement metadata if present and keep implicit local tasks filterable as a stable local/default target value without redesigning Phase 8
- Per D-08, ignore transcripts entirely and never derive events from transcript text

Projection scope:
- include a root run-created event from `CoordinatorRun.CreatedAt`
- include task-created / routed events from child-task artifacts and routing-decision presence
- include review, blocker, blocker-resolution, and partial-replan events from durable coordinator artifacts
- include task lifecycle transitions from `task.accept`, `task.wait`, `task.block`, `task.done`, and `review.respond`
- assign each event a deterministic timestamp, event kind, task/message refs, and compact summary string for later terminal rendering
- sort ascending by timestamp, then deterministic precedence, then stable IDs so repeated rebuilds are byte-stable

Keep this file UI-free. Do not modify `run show`, `log`, or Cobra surfaces in this plan.
  </action>
  <verify>
    <automated>go test ./internal/session -run 'TestBuildRunTimelineIncludesRoutingReviewBlockerReplanAndStateTransitions|TestBuildRunTimelineSortsDeterministicallyWhenTimestampsCollide|TestBuildRunTimelineRejectsStateEventAgentMismatch|TestFilterRunTimelineByOwnerStateClassAndExecutionTarget' -count=1</automated>
  </verify>
  <done>The repo has a strict, reusable timeline read model over canonical run artifacts plus state events, ready for operator-facing view integration in the next plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| durable YAML/JSONL -> timeline projection | Operator-visible history is rebuilt from mutable on-disk artifacts and event logs |
| Phase 8 placement metadata -> filter fields | Execution-target labels cross from persisted task placement into timeline filter keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | T | `BuildRunTimeline` | mitigate | Validate matched `TaskEvent` rows against canonical task/message/thread ownership and fail loudly on drift per D-03 |
| T-09-02 | I | timeline filter metadata | mitigate | Populate owner/state/class/target fields only from durable task/artifact data, never from transcript text or pane names |
| T-09-03 | D | deterministic ordering | mitigate | Apply explicit secondary sort keys so repeated rebuilds cannot degrade into unstable output ordering |
</threat_model>

<verification>
- `go test ./internal/session -run 'TestBuildRunTimelineIncludesRoutingReviewBlockerReplanAndStateTransitions|TestBuildRunTimelineSortsDeterministicallyWhenTimestampsCollide|TestBuildRunTimelineRejectsStateEventAgentMismatch|TestFilterRunTimelineByOwnerStateClassAndExecutionTarget' -count=1`
- `go test ./internal/session -count=1`
</verification>

<success_criteria>
- A new pure timeline read-model module exists under `internal/session` and is built only from canonical run artifacts plus `state.jsonl`.
- Timeline tests prove coverage for routing, review, blocker, replan, and task-state transitions.
- Execution-target filter fields come from persisted placement metadata, with deterministic local fallback semantics.
- Timeline rebuild errors on artifact/event disagreement instead of rendering guessed history.
</success_criteria>

<output>
After completion, create `.planning/phases/09-run-timeline-views/09-01-SUMMARY.md`
</output>
