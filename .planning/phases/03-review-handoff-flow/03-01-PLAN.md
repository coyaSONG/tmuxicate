---
phase: 03-review-handoff-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/protocol/coordinator.go
  - internal/protocol/validation.go
  - internal/mailbox/paths.go
  - internal/mailbox/coordinator_store.go
  - internal/session/run.go
  - internal/session/task_cmd.go
  - internal/session/task_cmd_test.go
autonomous: true
requirements:
  - REVIEW-01
must_haves:
  truths:
    - Completing a routed implementation task with review_required=true writes exactly one review handoff artifact after the source receipt is durable in done.
    - The linked follow-up review task is created through RouteChildTask and delivered as a review_request message without adding reverse-pointer fields to child task YAML.
    - If review handoff creation cannot route or persist, the source implementation task stays done and the handoff artifact records a fail-loud failure summary.
  artifacts:
    - path: internal/protocol/coordinator.go
      provides: ReviewHandoff contract plus review outcome and status enums
    - path: internal/mailbox/coordinator_store.go
      provides: review handoff CRUD and coordinator task lookup helpers
    - path: internal/session/task_cmd.go
      provides: post-done review handoff orchestration
    - path: internal/session/task_cmd_test.go
      provides: direct session coverage for handoff create, idempotency, and fail-loud failure paths
  key_links:
    - from: internal/session/task_cmd.go
      to: internal/mailbox/coordinator_store.go
      via: ReadTask, CreateReviewHandoff, ReadReviewHandoff, and UpdateReviewHandoff
      pattern: CreateReviewHandoff|ReadReviewHandoff|UpdateReviewHandoff|ReadTask
    - from: internal/session/task_cmd.go
      to: internal/session/run.go
      via: RouteChildTask(TaskClassReview)
      pattern: RouteChildTask\(
    - from: internal/session/run.go
      to: internal/protocol/envelope.go
      via: review-class child tasks emit KindReviewRequest instead of KindTask
      pattern: KindReviewRequest
---

<objective>
Create the automatic post-completion review handoff for coordinator implementation tasks that require review.

Purpose: Deliver `REVIEW-01` with a deterministic, disk-backed handoff that stays inside the existing run/task/mailbox workflow and fails loudly instead of silently skipping review.
Output: Validated `ReviewHandoff` artifacts under `coordinator/runs/<run-id>/reviews/`, routed review-request child tasks, and direct `internal/session` coverage for create/idempotent/failure behavior.
</objective>

<execution_context>
@/Users/chsong/.codex/get-shit-done/workflows/execute-plan.md
@/Users/chsong/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-review-handoff-flow/03-CONTEXT.md
@.planning/phases/03-review-handoff-flow/03-RESEARCH.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/TESTING.md
@DESIGN.md
@internal/protocol/coordinator.go
@internal/protocol/validation.go
@internal/mailbox/paths.go
@internal/mailbox/coordinator_store.go
@internal/session/task_cmd.go
@internal/session/run.go

<interfaces>
From `internal/session/task_cmd.go`:
```go
func TaskDone(stateDir, agent string, msgID protocol.MessageID, summary string) error
```

From `internal/session/run.go`:
```go
func RouteChildTask(cfg *config.ResolvedConfig, store *mailbox.Store, req protocol.RouteChildTaskRequest) (*protocol.ChildTask, *protocol.RoutingDecision, error)
```

From `internal/protocol/coordinator.go`:
```go
type ChildTask struct {
	TaskID            TaskID
	ParentRunID       RunID
	Owner             AgentName
	ReviewRequired    bool
	TaskClass         TaskClass
	Domains           []string
	NormalizedDomains []string
	DuplicateKey      string
	RoutingDecision   *RoutingDecision
	MessageID         MessageID
	ThreadID          ThreadID
}

type RouteChildTaskRequest struct {
	RunID          RunID
	TaskClass      TaskClass
	Domains        []string
	Goal           string
	ExpectedOutput string
	ReviewRequired bool
	OwnerOverride  AgentName
	OverrideReason string
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add review handoff contracts, store seams, and red session tests</name>
  <files>
internal/protocol/coordinator.go
internal/protocol/validation.go
internal/mailbox/paths.go
internal/mailbox/coordinator_store.go
internal/session/task_cmd_test.go
  </files>
  <read_first>
.planning/phases/03-review-handoff-flow/03-CONTEXT.md
.planning/phases/03-review-handoff-flow/03-RESEARCH.md
.planning/codebase/TESTING.md
internal/protocol/coordinator.go
internal/protocol/validation.go
internal/mailbox/paths.go
internal/mailbox/coordinator_store.go
internal/session/task_cmd.go
internal/session/run.go
internal/session/run_test.go
  </read_first>
  <behavior>
    - Completing a routed implementation task with `review_required=true` should create one canonical review handoff file at `coordinator/runs/<run-id>/reviews/<source-task-id>.yaml`.
    - A second handoff attempt for the same source task should reuse the existing handoff file instead of creating a duplicate review task.
    - Missing routing prerequisites such as empty `normalized_domains` should record `status: handoff_failed` and leave the source task completion intact.
  </behavior>
  <acceptance_criteria>
    - `rg -n "type ReviewHandoff struct|type ReviewOutcome string|type ReviewHandoffStatus string" internal/protocol/coordinator.go` returns matches.
    - `rg -n "source_task_id|source_message_id|review_task_id|review_message_id|reviewer|status|failure_summary|response_message_id|outcome|responded_at" internal/protocol/coordinator.go` returns matches inside `ReviewHandoff`.
    - `rg -n "func RunReviewsDir|func RunReviewHandoffPath" internal/mailbox/paths.go` returns two matches.
    - `rg -n "func \\(s \\*CoordinatorStore\\) ReadTask|CreateReviewHandoff|ReadReviewHandoff|UpdateReviewHandoff" internal/mailbox/coordinator_store.go` returns matches.
    - `type ChildTask struct` in `internal/protocol/coordinator.go` does not gain `source_task_id`, `linked_review_task_id`, or `review_handoff_id` fields; linkage lives only on `ReviewHandoff` per D-06.
    - `rg -n "TestTaskDoneCreatesReviewHandoffAndRoutesReview|TestTaskDoneReviewHandoffIsIdempotent|TestTaskDoneRecordsReviewHandoffFailureWithoutRollback" internal/session/task_cmd_test.go` returns three matches.
  </acceptance_criteria>
  <action>
Create the review-handoff contract in `internal/protocol/coordinator.go` as a dedicated coordinator artifact per D-05, D-07, and D-12. Add `type ReviewOutcome string` with exact values `approved` and `changes_requested`, and `type ReviewHandoffStatus string` with exact values `pending`, `responded`, and `handoff_failed`. Define `type ReviewHandoff struct` with YAML fields `run_id`, `source_task_id`, `source_message_id`, `review_task_id`, `review_message_id`, `reviewer`, `status`, `failure_summary`, `response_message_id`, `outcome`, `created_at`, and `responded_at`.

Add validation in `internal/protocol/validation.go` so:
- every handoff requires `run_id`, `source_task_id`, `source_message_id`, `status`, and `created_at`
- `status=pending` requires `review_task_id`, `review_message_id`, and `reviewer`
- `status=handoff_failed` requires non-empty `failure_summary`
- `status=responded` requires `review_task_id`, `review_message_id`, `reviewer`, `response_message_id`, `outcome`, and `responded_at`
- `ChildTask` stays unchanged as the canonical non-link task schema per D-06

Add path helpers `RunReviewsDir(stateDir, runID)` and `RunReviewHandoffPath(stateDir, runID, sourceTaskID)` in `internal/mailbox/paths.go`. Extend `internal/mailbox/coordinator_store.go` with `ReadTask(runID, taskID)`, `CreateReviewHandoff(handoff)`, `ReadReviewHandoff(runID, sourceTaskID)`, and `UpdateReviewHandoff(runID, sourceTaskID, updateFn)` using the same YAML marshal + atomic write pattern as `CreateRun` and `CreateTask`.

Create `internal/session/task_cmd_test.go` with red tests named exactly:
- `TestTaskDoneCreatesReviewHandoffAndRoutesReview`
- `TestTaskDoneReviewHandoffIsIdempotent`
- `TestTaskDoneRecordsReviewHandoffFailureWithoutRollback`

Seed tests with a routed implementation task (`TaskClassImplementation`, normalized domains present, `review_required=true`) and assert on-disk handoff YAML at `reviews/<source-task-id>.yaml`, not on transcript text or duplicate-key scanning, per D-02, D-04, and D-08.
  </action>
  <verify>
    <automated>go test ./internal/session -run 'TestTaskDoneCreatesReviewHandoffAndRoutesReview|TestTaskDoneReviewHandoffIsIdempotent|TestTaskDoneRecordsReviewHandoffFailureWithoutRollback' -count=1</automated>
  </verify>
  <done>
The review handoff contract, path/store helpers, and the three red session tests exist. The test command is expected to fail until Task 2 implements the post-`done` handoff behavior.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement post-done review handoff creation and fail-loud routing</name>
  <files>
internal/session/task_cmd.go
internal/session/run.go
internal/session/task_cmd_test.go
  </files>
  <read_first>
.planning/phases/03-review-handoff-flow/03-CONTEXT.md
.planning/phases/03-review-handoff-flow/03-RESEARCH.md
internal/session/task_cmd.go
internal/session/run.go
internal/session/read_msg.go
internal/session/run_contracts.go
internal/mailbox/coordinator_store.go
internal/protocol/coordinator.go
internal/protocol/envelope.go
internal/session/task_cmd_test.go
  </read_first>
  <behavior>
    - `TaskDone` should trigger review handoff only after the source receipt is moved from `active` to `done`.
    - Review-required implementation tasks should route exactly one follow-up review task through `RouteChildTask(TaskClassReview)` and persist the linkage on `ReviewHandoff`.
    - Route failures, missing normalized domains, or handoff persistence failures should record `status=handoff_failed` on the handoff artifact and must not roll back the source task's `done` state.
  </behavior>
  <acceptance_criteria>
    - `rg -n "KindReviewRequest" internal/session/run.go` returns a match in review-task workflow message creation.
    - `rg -n "TaskClassReview|handoff_failed|failure_summary|review_required" internal/session/task_cmd.go` returns matches in the new handoff logic.
    - `go test ./internal/session -run 'TestTaskDoneCreatesReviewHandoffAndRoutesReview|TestTaskDoneReviewHandoffIsIdempotent|TestTaskDoneRecordsReviewHandoffFailureWithoutRollback' -count=1` passes.
    - `go test ./internal/session ./internal/mailbox ./internal/protocol -count=1` passes.
  </acceptance_criteria>
  <action>
Extend `TaskDone` in `internal/session/task_cmd.go` only after the existing durable completion sequence succeeds: keep the existing `UpdateReceipt(...DoneAt...)` and `MoveReceipt(active -> done)` order intact per D-01, then branch into a new helper for coordinator review handoff.

Use `store.ReadMessage(msgID)` to inspect message metadata and return the existing `task.done` behavior unchanged when the message is not a coordinator child task. When `meta.parent_run_id` and `meta.task_id` exist, load the source task through `CoordinatorStore.ReadTask`. Continue only when `sourceTask.TaskClass == protocol.TaskClassImplementation` and `sourceTask.ReviewRequired == true`; all other tasks, including review tasks, must return without creating a handoff. This keeps Phase 3 scoped to implementation -> review and prevents recursive review-of-review loops by routing the follow-up review task with `ReviewRequired: false`.

Make handoff idempotency artifact-based per D-04 and D-08: check `ReadReviewHandoff(runID, sourceTaskID)` first and return without re-routing when the handoff file already exists. When `sourceTask.NormalizedDomains` is empty, create or update the handoff file with `status: handoff_failed` and `failure_summary: "source task <task-id> missing normalized_domains for review routing"` and return nil.

For successful handoff creation, create a pending `ReviewHandoff`, then call `RouteChildTask` with:
- `TaskClass: protocol.TaskClassReview`
- `Domains: sourceTask.NormalizedDomains`
- `Goal: "Review implementation task <source-task-id>: <sourceTask.Goal>"`
- `ExpectedOutput: "Submit approved or changes_requested for <source-task-id> via tmuxicate review respond"`
- `ReviewRequired: false`

On success, update the same handoff with `review_task_id`, `review_message_id`, `reviewer`, and `status: pending`. On any route or artifact-update error after the source task is already done, update the handoff to `status: handoff_failed` plus `failure_summary` and return nil so D-03 is satisfied.

In `internal/session/run.go`, make `addChildTaskWithResolvedOwner` emit `protocol.KindReviewRequest` whenever `task.TaskClass == protocol.TaskClassReview`; keep all non-review child tasks as `protocol.KindTask`. Do not add `source_task_id`, `review_task_id`, or similar reverse-pointer fields to `ChildTask` or message metadata; the sole canonical linkage remains `reviews/<source-task-id>.yaml` per D-05 and D-06.
  </action>
  <verify>
    <automated>go test ./internal/session -run 'TestTaskDoneCreatesReviewHandoffAndRoutesReview|TestTaskDoneReviewHandoffIsIdempotent|TestTaskDoneRecordsReviewHandoffFailureWithoutRollback' -count=1 && go test ./internal/session ./internal/mailbox ./internal/protocol -count=1</automated>
  </verify>
  <done>
Review-required implementation task completion now creates one canonical review handoff plus one linked `review_request` task, repeated calls do not duplicate handoffs, and failure paths preserve the source task's `done` state while recording `handoff_failed` with a readable failure summary.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent task completion CLI -> `internal/session/task_cmd.go` | untrusted `message_id`, `agent`, and optional summary enter the review-handoff workflow here |
| session workflow -> coordinator artifacts under `coordinator/runs/<run-id>/reviews/` | durable review-chain state is created or updated here and must stay canonical |
| session workflow -> routed mailbox message creation | review handoff emits a new reviewer-facing message that must preserve run lineage and kind semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | `internal/session/task_cmd.go` | mitigate | Create the handoff only after the existing `UpdateReceipt` + `MoveReceipt(active -> done)` sequence succeeds; load the source task from durable `parent_run_id` + `task_id` metadata and ignore non-implementation or non-review-required tasks. |
| T-03-02 | T | `internal/mailbox/coordinator_store.go` | mitigate | Use `RunReviewHandoffPath(runID, sourceTaskID)` as the only uniqueness gate; check `ReadReviewHandoff` before routing and never infer review linkage from duplicate keys, prompt text, or transcript scanning. |
| T-03-03 | D | `internal/session/task_cmd.go` | mitigate | When domains are missing or routing/persistence fails, keep the source receipt in `done` and persist `status: handoff_failed` plus `failure_summary` on the handoff artifact so review cannot disappear silently. |
</threat_model>

<verification>
- `go test ./internal/session -run 'TestTaskDoneCreatesReviewHandoffAndRoutesReview|TestTaskDoneReviewHandoffIsIdempotent|TestTaskDoneRecordsReviewHandoffFailureWithoutRollback' -count=1`
- `go test ./internal/session ./internal/mailbox ./internal/protocol -count=1`
</verification>

<success_criteria>
- `internal/protocol/coordinator.go` contains the canonical `ReviewHandoff` contract and exact outcome values `approved` / `changes_requested`.
- `internal/mailbox/paths.go` exposes `RunReviewsDir` and `RunReviewHandoffPath`, and `internal/mailbox/coordinator_store.go` can create, read, and update those artifacts plus read a child task by run/task ID.
- `TaskDone` creates one routed review handoff after source completion, records `handoff_failed` without rollback when routing prerequisites are missing, and review tasks are emitted as `review_request`.
- The targeted and package regression commands in `<verification>` pass.
</success_criteria>

<output>
After completion, create `.planning/phases/03-review-handoff-flow/03-01-SUMMARY.md`
</output>
