---
phase: 06-adaptive-routing-signals
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - cmd/tmuxicate/main.go
  - cmd/tmuxicate/main_test.go
  - internal/protocol/coordinator.go
  - internal/protocol/validation.go
  - internal/session/run.go
  - internal/session/run_test.go
  - internal/session/run_adaptive.go
  - internal/session/run_adaptive_test.go
  - internal/session/run_rebuild.go
  - internal/session/run_rebuild_test.go
autonomous: true
requirements:
  - ADAPT-01
  - ADAPT-02
must_haves:
  truths:
    - "Adaptive routing only reorders owners inside the existing deterministic candidate set; it never bypasses allowed owners, role/domain matching, or duplicate policy."
    - "Each routed child task persists enough adaptive explanation data to show the baseline owner, the applied preference score, and the historical/manual evidence that changed the outcome."
    - "Operators can inspect adaptive routing evidence from existing routing surfaces, specifically the `route-task` command output and `run show`, without transcript review."
  artifacts:
    - path: internal/session/run.go
      provides: "Adaptive-aware owner ranking layered onto the existing `RouteChildTask` deterministic routing flow"
    - path: internal/protocol/coordinator.go
      provides: "Canonical adaptive decision explanation fields persisted on routing decisions"
    - path: internal/session/run_rebuild.go
      provides: "`run show` rendering of adaptive routing explanations alongside existing routing evidence"
    - path: cmd/tmuxicate/main.go
      provides: "`route-task` command output that prints adaptive decision evidence at selection time"
    - path: internal/session/run_test.go
      provides: "Direct coverage for adaptive ranking, baseline fallback, and deterministic tie behavior"
    - path: internal/session/run_rebuild_test.go
      provides: "Regression coverage that adaptive evidence survives disk rebuild and appears in operator views"
  key_links:
    - from: internal/session/run_adaptive.go
      to: internal/session/run.go
      via: "Coordinator-scoped adaptive preference artifacts are loaded and matched by exact task class plus normalized domains before final ranking"
      pattern: "ReadAdaptiveRoutingPreferences|matchAdaptivePreference|RouteChildTask"
    - from: internal/session/run.go
      to: internal/protocol/coordinator.go
      via: "The routed task persists a structured adaptive explanation on the canonical routing decision"
      pattern: "RoutingDecision|AdaptiveRoutingExplanation"
    - from: internal/protocol/coordinator.go
      to: internal/session/run_rebuild.go
      via: "Persisted adaptive explanation fields drive `run show` output without any second reporting backend"
      pattern: "AdaptiveRoutingExplanation|FormatRunGraph"
---

<objective>
Apply the persisted adaptive routing preferences inside `route-task` and expose decision evidence in the existing operator views.

Purpose: Finish Phase 6 by reusing prior-run signals without making routing opaque, preserving the Phase 2 deterministic baseline while satisfying D-03 through D-06.
Output: Adaptive-aware `RouteChildTask` ranking, persisted adaptive decision explanations on routed tasks, and operator-visible evidence in `route-task` output plus `run show`.
</objective>

<execution_context>
@/Users/chsong/.codex/get-shit-done/workflows/execute-plan.md
@/Users/chsong/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-adaptive-routing-signals/06-CONTEXT.md
@.planning/phases/06-adaptive-routing-signals/06-01-PLAN.md
@.planning/milestones/v1.0-phases/02-role-based-routing/02-02-SUMMARY.md
@.planning/milestones/v1.0-phases/05-run-summaries/05-02-SUMMARY.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@cmd/tmuxicate/main.go
@internal/protocol/coordinator.go
@internal/protocol/validation.go
@internal/session/run.go
@internal/session/run_adaptive.go
@internal/session/run_rebuild.go

<interfaces>
From Phase 06-01 deliverables:
```go
type AdaptiveRoutingPreferenceSet struct {
	Coordinator  AgentName                  `yaml:"coordinator"`
	UpdatedAt    time.Time                  `yaml:"updated_at"`
	LookbackRuns int                        `yaml:"lookback_runs"`
	Preferences  []AdaptiveRoutingPreference `yaml:"preferences"`
}

type AdaptiveRoutingPreference struct {
	PreferenceKey     string     `yaml:"preference_key"`
	TaskClass         TaskClass  `yaml:"task_class"`
	NormalizedDomains []string   `yaml:"normalized_domains"`
	PreferredOwner    AgentName  `yaml:"preferred_owner"`
	HistoricalScore   int        `yaml:"historical_score"`
	ManualWeight      int        `yaml:"manual_weight"`
	TotalScore        int        `yaml:"total_score"`
	Evidence          []AdaptiveRoutingEvidenceRef `yaml:"evidence"`
}
```

Current routing seam from internal/session/run.go:
```go
func RouteChildTask(cfg *config.ResolvedConfig, store *mailbox.Store, req protocol.RouteChildTaskRequest) (*protocol.ChildTask, *protocol.RoutingDecision, error)
```

Current operator rebuild seam from internal/session/run_rebuild.go:
```go
func LoadRunGraph(stateDir string, runID protocol.RunID) (*RunGraph, error)
func FormatRunGraph(graph *RunGraph) string
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add red tests for adaptive ranking, deterministic fallback, and operator-visible evidence</name>
  <files>cmd/tmuxicate/main_test.go, internal/session/run_test.go, internal/session/run_rebuild_test.go</files>
  <read_first>
    .planning/phases/06-adaptive-routing-signals/06-CONTEXT.md
    .planning/phases/06-adaptive-routing-signals/06-01-PLAN.md
    .planning/milestones/v1.0-phases/02-role-based-routing/02-02-SUMMARY.md
    .planning/milestones/v1.0-phases/05-run-summaries/05-02-SUMMARY.md
    internal/protocol/coordinator.go
    internal/session/run.go
    internal/session/run_adaptive.go
    internal/session/run_rebuild.go
    cmd/tmuxicate/main.go
  </read_first>
  <behavior>
    - Per D-03, adaptive signals may only affect ranking after the existing route baseline has already filtered candidates by allowed owners, task class, domains, and duplicate policy.
    - Per D-04, every adaptive decision must persist and render the baseline owner, applied score, and evidence refs that shifted the result.
    - Per D-05 and D-06, the operator-facing explanation must appear in current routing surfaces instead of a disconnected new reporting command.
  </behavior>
  <action>
Add these exact failing tests.

In `internal/session/run_test.go`:
- `TestRouteChildTaskAppliesAdaptivePreferenceWithinEligibleCandidates` sets up two eligible implementation owners where baseline routing would choose `backend-fast` by `route_priority`, but the persisted adaptive preference artifact favors `backend-steady` for the exact normalized domains and must shift selection only because `backend-steady` remains baseline-eligible.
- `TestRouteChildTaskFallsBackToBaselineWhenAdaptiveScoresTieOrMissing` covers two cases: no preference artifact for the request key, and equal `total_score` values for both eligible owners. In both cases the result must stay `route_priority desc, config_order asc`.
- `TestRouteChildTaskPersistsAdaptiveExplanation` asserts the returned task and routing decision now contain exact fields `baseline_owner`, `historical_score`, `manual_weight`, `total_score`, `reason`, and at least one `evidence` ref.

In `internal/session/run_rebuild_test.go`:
- `TestFormatRunGraphIncludesAdaptiveRoutingExplanation` asserts a rebuilt task renders exact labels `Adaptive Routing:`, `Adaptive Baseline:`, `Adaptive Score:`, and `Adaptive Evidence:` when adaptive explanation data exists.

In `cmd/tmuxicate/main_test.go`:
- `TestRunRouteTaskCommandPrintsAdaptiveDecisionEvidence` executes the Cobra `run route-task` path and asserts stdout includes the selected owner plus the same exact labels `Adaptive Routing:`, `Adaptive Baseline:`, and `Adaptive Score:`.

Make the red tests require exact-match preference keys on `task_class + normalized_domains`; they must fail if adaptive ranking uses fuzzy domain overlap or bypasses duplicate/no-match safeguards.
  </action>
  <acceptance_criteria>
    - `rg -n "TestRouteChildTaskAppliesAdaptivePreferenceWithinEligibleCandidates|TestRouteChildTaskFallsBackToBaselineWhenAdaptiveScoresTieOrMissing|TestRouteChildTaskPersistsAdaptiveExplanation|TestFormatRunGraphIncludesAdaptiveRoutingExplanation|TestRunRouteTaskCommandPrintsAdaptiveDecisionEvidence" internal/session/run_test.go internal/session/run_rebuild_test.go cmd/tmuxicate/main_test.go` returns all five test names.
    - `internal/session/run_test.go` contains the exact field names `baseline_owner`, `historical_score`, `manual_weight`, `total_score`, and `reason`.
    - `internal/session/run_rebuild_test.go` and `cmd/tmuxicate/main_test.go` contain the exact labels `Adaptive Routing:`, `Adaptive Baseline:`, `Adaptive Score:`, and `Adaptive Evidence:`.
    - `go test ./cmd/tmuxicate ./internal/session -run 'TestRouteChildTaskAppliesAdaptivePreferenceWithinEligibleCandidates|TestRouteChildTaskFallsBackToBaselineWhenAdaptiveScoresTieOrMissing|TestRouteChildTaskPersistsAdaptiveExplanation|TestFormatRunGraphIncludesAdaptiveRoutingExplanation|TestRunRouteTaskCommandPrintsAdaptiveDecisionEvidence' -count=1` fails before the adaptive routing implementation lands.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd/tmuxicate ./internal/session -run 'TestRouteChildTaskAppliesAdaptivePreferenceWithinEligibleCandidates|TestRouteChildTaskFallsBackToBaselineWhenAdaptiveScoresTieOrMissing|TestRouteChildTaskPersistsAdaptiveExplanation|TestFormatRunGraphIncludesAdaptiveRoutingExplanation|TestRunRouteTaskCommandPrintsAdaptiveDecisionEvidence' -count=1</automated>
  </verify>
  <done>Adaptive ranking and evidence expectations are pinned in red tests before route-task or operator-view code changes land.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement adaptive-aware route-task ranking and operator-visible decision evidence</name>
  <files>cmd/tmuxicate/main.go, cmd/tmuxicate/main_test.go, internal/protocol/coordinator.go, internal/protocol/validation.go, internal/session/run.go, internal/session/run_test.go, internal/session/run_adaptive.go, internal/session/run_adaptive_test.go, internal/session/run_rebuild.go, internal/session/run_rebuild_test.go</files>
  <read_first>
    .planning/phases/06-adaptive-routing-signals/06-CONTEXT.md
    .planning/phases/06-adaptive-routing-signals/06-01-PLAN.md
    .planning/milestones/v1.0-phases/02-role-based-routing/02-02-SUMMARY.md
    .planning/milestones/v1.0-phases/05-run-summaries/05-02-SUMMARY.md
    internal/protocol/coordinator.go
    internal/protocol/validation.go
    internal/session/run.go
    internal/session/run_adaptive.go
    internal/session/run_rebuild.go
    cmd/tmuxicate/main.go
  </read_first>
  <action>
Implement adaptive routing application with these exact boundaries.

Deterministic ranking, per D-02 and D-03:
- In `internal/session/run_adaptive.go`, add narrow helpers that load the coordinator-scoped adaptive preference artifact and match only exact `(task_class, normalized_domains, preferred_owner)` rows.
- In `internal/session/run.go`, keep the existing route baseline unchanged:
  1. duplicate check first,
  2. allowed-owner/task-class/domain filtering second,
  3. baseline order by `route_priority desc, config_order asc`,
  4. adaptive overlay only among the already-eligible domain candidates.
- Rank adaptive candidates by `total_score desc`, then `route_priority desc`, then `config_order asc`.
- If no exact-match adaptive preference exists, or if the highest `total_score` ties, preserve the current baseline winner with no behavioral drift.
- Owner overrides must still stay inside the run's allowed owners and must still not bypass duplicate blocking. Adaptive preferences do not change that path.

Persisted explanation, per D-04:
- Extend `RoutingDecision` in `internal/protocol/coordinator.go` with `Adaptive *AdaptiveRoutingExplanation`.
- Add `AdaptiveRoutingExplanation` with exact fields `applied`, `baseline_owner`, `preferred_owner`, `historical_score`, `manual_weight`, `total_score`, `reason`, and `evidence`.
- Validate the new explanation struct in `internal/protocol/validation.go`; require `baseline_owner` and `reason` when `applied` is true, and require sorted evidence refs.
- When adaptive routing changes the winner, persist:
  - the baseline owner before adaptive overlay,
  - the preferred owner actually selected,
  - the exact score breakdown,
  - the copied evidence refs from the matched adaptive preference row,
  - a stable human-readable `reason` string such as `exact preference match on implementation|protocol,session favored backend-steady over baseline backend-fast`.
- When no adaptive preference applies, either omit the explanation or persist `applied: false` with the baseline owner only. Keep the task YAML and route rejection format backward compatible.

Operator surfaces, per D-05 and D-06:
- In `cmd/tmuxicate/main.go`, extend `run route-task` output so successful routing prints the selected owner and, when present, the adaptive explanation lines `Adaptive Routing:`, `Adaptive Baseline:`, `Adaptive Score:`, and `Adaptive Evidence:`.
- In `internal/session/run_rebuild.go`, render the same explanation under the task block using the existing routing-evidence area, not a new run-level report section.
- Keep the current `Routing Decision:` and `Candidates:` lines intact; adaptive explanation is additive, not a replacement.

Do not introduce probabilistic scoring, fuzzy domain matching, or a new dashboard/reporting command in this plan.
  </action>
  <acceptance_criteria>
    - `rg -n "type AdaptiveRoutingExplanation struct|Adaptive \\*AdaptiveRoutingExplanation" internal/protocol/coordinator.go` returns the new persisted explanation contract.
    - `rg -n "baseline_owner|preferred_owner|historical_score|manual_weight|total_score|exact preference match" internal/protocol/coordinator.go internal/session/run.go` returns the adaptive explanation fields and stable reason text.
    - `rg -n "Adaptive Routing:|Adaptive Baseline:|Adaptive Score:|Adaptive Evidence:" cmd/tmuxicate/main.go internal/session/run_rebuild.go` returns all operator-visible evidence labels.
    - `go test ./cmd/tmuxicate ./internal/session ./internal/protocol -run 'TestRouteChildTaskAppliesAdaptivePreferenceWithinEligibleCandidates|TestRouteChildTaskFallsBackToBaselineWhenAdaptiveScoresTieOrMissing|TestRouteChildTaskPersistsAdaptiveExplanation|TestFormatRunGraphIncludesAdaptiveRoutingExplanation|TestRunRouteTaskCommandPrintsAdaptiveDecisionEvidence' -count=1` passes.
    - `go test ./cmd/tmuxicate ./internal/config ./internal/mailbox ./internal/session ./internal/protocol -count=1` passes after the targeted tests pass.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd/tmuxicate ./internal/session ./internal/protocol -run 'TestRouteChildTaskAppliesAdaptivePreferenceWithinEligibleCandidates|TestRouteChildTaskFallsBackToBaselineWhenAdaptiveScoresTieOrMissing|TestRouteChildTaskPersistsAdaptiveExplanation|TestFormatRunGraphIncludesAdaptiveRoutingExplanation|TestRunRouteTaskCommandPrintsAdaptiveDecisionEvidence' -count=1 && go test ./cmd/tmuxicate ./internal/config ./internal/mailbox ./internal/session ./internal/protocol -count=1</automated>
  </verify>
  <done>`route-task` now reuses persisted adaptive preferences without bypassing the existing deterministic routing baseline, and operators can inspect the resulting rationale from the current routing and run-inspection surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adaptive preference artifact -> route selection | Cross-run preferences influence current owner ranking |
| route baseline -> adaptive overlay | Existing duplicate/allowed-owner/domain rules must remain authoritative |
| persisted routing decision -> operator views | Route explanations shown to operators must match the durable task YAML exactly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | T | `internal/session/run.go` | mitigate | Apply adaptive ranking only after the existing baseline candidate filter succeeds, and fall back to baseline when scores tie or no exact match exists. |
| T-06-07 | E | owner selection | mitigate | Keep owner overrides and adaptive choices inside the run's allowed-owner snapshot; adaptive preference rows cannot nominate agents outside the existing route baseline. |
| T-06-08 | R | `RoutingDecision` / `run show` | mitigate | Persist a structured adaptive explanation with baseline owner, score breakdown, reason, and evidence refs so CLI output can be reconstructed from disk. |
| T-06-09 | I | operator surfaces | accept | Showing adaptive evidence duplicates already-durable coordinator metadata and does not reveal secrets beyond existing task/run artifacts. |
</threat_model>

<verification>
Run `go test ./cmd/tmuxicate ./internal/session ./internal/protocol -run 'TestRouteChildTaskAppliesAdaptivePreferenceWithinEligibleCandidates|TestRouteChildTaskFallsBackToBaselineWhenAdaptiveScoresTieOrMissing|TestRouteChildTaskPersistsAdaptiveExplanation|TestFormatRunGraphIncludesAdaptiveRoutingExplanation|TestRunRouteTaskCommandPrintsAdaptiveDecisionEvidence' -count=1` after both tasks. Then run `go test ./cmd/tmuxicate ./internal/config ./internal/mailbox ./internal/session ./internal/protocol -count=1` before closing the plan.
</verification>

<success_criteria>
- Adaptive routing changes selection only when an exact-match persisted preference beats the baseline winner inside the existing eligible candidate set.
- Routed child tasks persist additive adaptive explanation fields that cite baseline owner, score breakdown, and evidence refs.
- Operators can inspect the adaptive rationale from both `tmuxicate run route-task` output and `tmuxicate run show` without needing transcript review or a new reporting command.
</success_criteria>

<output>
After completion, create `.planning/phases/06-adaptive-routing-signals/06-02-SUMMARY.md`
</output>
