---
phase: 04-blocker-escalation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/protocol/coordinator.go
  - internal/protocol/validation.go
  - internal/protocol/protocol_test.go
  - internal/mailbox/paths.go
  - internal/mailbox/coordinator_store.go
  - internal/mailbox/coordinator_store_test.go
  - internal/config/config.go
  - internal/config/loader.go
  - internal/config/loader_test.go
autonomous: true
requirements:
  - BLOCK-01
  - BLOCK-02
  - BLOCK-03
must_haves:
  truths:
    - Every blocked logical work item has one durable blocker record the operator can inspect even after reroutes, because the artifact stays keyed to the source task rather than the current owner.
    - Every coordinator blocker case carries explicit structured next-step handling and escalation context; freeform reason text explains the case but never decides policy.
    - Automatic reroutes stop at an explicit blocker ceiling from `blockers.*`, so unresolved work surfaces to the operator instead of looping behind delivery retry settings.
  artifacts:
    - path: internal/protocol/coordinator.go
      provides: validated blocker enums, `BlockerCase`, and structured escalation/resolution data
    - path: internal/mailbox/coordinator_store.go
      provides: blocker-case CRUD plus lookup by current task for reroute continuity
    - path: internal/config/config.go
      provides: blocker-specific reroute ceiling config
    - path: internal/config/loader_test.go
      provides: regression coverage for blocker ceiling parsing and validation
  key_links:
    - from: internal/protocol/coordinator.go
      to: internal/protocol/validation.go
      via: blocker enum and artifact validation
      pattern: 'BlockerCase|WaitKind|BlockKind|BlockerAction'
    - from: internal/mailbox/paths.go
      to: internal/mailbox/coordinator_store.go
      via: canonical `coordinator/runs/<run-id>/blockers/<source-task-id>.yaml` paths
      pattern: 'RunBlockersDir|RunBlockerCasePath'
    - from: internal/config/config.go
      to: internal/config/loader.go
      via: `BlockersConfig` defaults and validation
      pattern: 'BlockersConfig|max_reroutes'
---

<objective>
Define the durable blocker contracts, blocker artifact storage, and blocker reroute ceiling config that every later Phase 4 implementation task depends on.

Purpose: Lock D-02, D-07, D-08, D-11, D-12, and D-13 into typed, validated, disk-backed contracts before any wait/block policy or CLI behavior is implemented.
Output: `BlockerCase` protocol schema, blocker-store helpers rooted under `coordinator/runs/<run-id>/blockers/`, and validated `blockers.max_reroutes_*` config support.
</objective>

<execution_context>
@/Users/chsong/.codex/get-shit-done/workflows/execute-plan.md
@/Users/chsong/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-blocker-escalation/04-CONTEXT.md
@.planning/phases/04-blocker-escalation/04-RESEARCH.md
@.planning/phases/04-blocker-escalation/04-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONCERNS.md
@internal/protocol/coordinator.go
@internal/protocol/validation.go
@internal/mailbox/paths.go
@internal/mailbox/coordinator_store.go
@internal/config/config.go
@internal/config/loader.go
@internal/runtime/daemon.go

<interfaces>
Existing workflow artifact pattern to mirror:
```go
type ReviewHandoff struct {
	RunID             RunID
	SourceTaskID      TaskID
	SourceMessageID   MessageID
	ReviewTaskID      TaskID
	ReviewMessageID   MessageID
	Reviewer          AgentName
	Status            ReviewHandoffStatus
	FailureSummary    string
	ResponseMessageID MessageID
	Outcome           ReviewOutcome
	CreatedAt         time.Time
	RespondedAt       *time.Time
}
```

Existing persistence/config seams to extend:
```go
type RoutingConfig struct {
	Coordinator          string
	ExclusiveTaskClasses []protocol.TaskClass
	FanoutTaskClasses    []protocol.TaskClass
}

func (c *Config) Validate() error
func (s *CoordinatorStore) CreateReviewHandoff(handoff *protocol.ReviewHandoff) error
func (s *CoordinatorStore) UpdateReviewHandoff(runID protocol.RunID, sourceTaskID protocol.TaskID, updateFn func(*protocol.ReviewHandoff) error) error
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add blocker protocol enums, artifact schema, and validation tests</name>
  <files>
internal/protocol/coordinator.go
internal/protocol/validation.go
internal/protocol/protocol_test.go
  </files>
  <read_first>
.planning/phases/04-blocker-escalation/04-CONTEXT.md
.planning/phases/04-blocker-escalation/04-RESEARCH.md
internal/protocol/coordinator.go
internal/protocol/validation.go
internal/protocol/protocol_test.go
  </read_first>
  <behavior>
    - `wait` and `block` stay distinct declared states per D-01, and only structured kinds may drive policy per D-02 and D-05.
    - Escalation must be artifact-backed with explicit `recommended_action`, not inferred from freeform text, per D-06, D-12, and D-13.
    - The blocker artifact must be keyed to `source_task_id`, while still carrying `current_task_id` / `current_message_id` so rerouted work can continue the same blocker chain per D-08.
  </behavior>
  <acceptance_criteria>
    - `rg -n "type BlockerCase struct|type WaitKind string|type BlockKind string|type BlockerAction string|type BlockerResolutionAction string" internal/protocol/coordinator.go` returns matches.
    - `rg -n "dependency_reply|external_event|agent_clarification|reroute_needed|human_decision|unsupported|watch|clarification_request|reroute|escalate|manual_reroute|clarify|dismiss" internal/protocol/coordinator.go` returns matches.
    - `rg -n "func \\(b \\*BlockerCase\\) Validate|func \\(w WaitKind\\) Validate|func \\(b BlockKind\\) Validate|func \\(a BlockerAction\\) Validate|func \\(a BlockerResolutionAction\\) Validate" internal/protocol/validation.go` returns matches.
    - `rg -n "TestBlockerCaseValidateRequiresStructuredKinds|TestBlockerCaseValidateRequiresRecommendedActionForEscalation|TestBlockerResolutionActionValidate" internal/protocol/protocol_test.go` returns matches.
  </acceptance_criteria>
  <action>
Add the blocker protocol surface to `internal/protocol/coordinator.go` with these exact enum/value sets:

- `WaitKind`: `dependency_reply`, `external_event`
- `BlockKind`: `agent_clarification`, `reroute_needed`, `human_decision`, `unsupported`
- `BlockerAction`: `watch`, `clarification_request`, `reroute`, `escalate`
- `BlockerStatus`: `active`, `escalated`, `resolved`
- `BlockerResolutionAction`: `manual_reroute`, `clarify`, `dismiss`

Add a `RecommendedAction` struct with `kind` and `note`, a `BlockerAttempt` struct for durable action history, a `BlockerResolution` struct for operator resolution, and a canonical `BlockerCase` artifact with at least these fields:
- `run_id`
- `source_task_id`
- `source_message_id`
- `source_owner`
- `current_task_id`
- `current_message_id`
- `current_owner`
- `declared_state`
- `wait_kind`
- `block_kind`
- `reason`
- `selected_action`
- `status`
- `reroute_count`
- `max_reroutes`
- `recommended_action`
- `resolution`
- `created_at`
- `updated_at`
- `escalated_at`
- `resolved_at`
- `attempts`

Implement validation in `internal/protocol/validation.go` with these exact rules:
- `declared_state` must be either `wait` or `block`
- `declared_state=wait` requires `wait_kind` and forbids `block_kind`
- `declared_state=block` requires `block_kind` and forbids `wait_kind`
- `status=escalated` requires non-nil `recommended_action.kind`
- `status=resolved` requires non-nil `resolution.action` and `resolved_at`
- `source_task_id` must always be generated `task_...`
- `current_task_id`, `resolution.created_task_id`, and any attempt task ids must either be empty or valid generated `task_...`
- `reroute_count` and `max_reroutes` must be `>= 0`

Add protocol tests in `internal/protocol/protocol_test.go` that pin the structured-kind requirement, the escalation/recommended-action requirement, and the exact `manual_reroute|clarify|dismiss` resolution action validation. Keep `reason` as explanatory only; never allow policy-critical behavior to depend on parsing it, per D-05.
  </action>
  <verify>
    <automated>go test ./internal/protocol -run 'TestBlockerCaseValidateRequiresStructuredKinds|TestBlockerCaseValidateRequiresRecommendedActionForEscalation|TestBlockerResolutionActionValidate' -count=1</automated>
  </verify>
  <done>
The repo has a validated blocker protocol contract with explicit wait/block subtype enums, durable escalation/resolution structures, and direct protocol tests covering the required state-specific invariants.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add canonical blocker-case paths, CRUD, and lookup-by-current-task tests</name>
  <files>
internal/mailbox/paths.go
internal/mailbox/coordinator_store.go
internal/mailbox/coordinator_store_test.go
  </files>
  <read_first>
.planning/phases/04-blocker-escalation/04-CONTEXT.md
.planning/phases/04-blocker-escalation/04-RESEARCH.md
internal/mailbox/paths.go
internal/mailbox/coordinator_store.go
internal/mailbox/store_test.go
  </read_first>
  <behavior>
    - The canonical blocker artifact path must be `coordinator/runs/<run-id>/blockers/<source-task-id>.yaml` per D-08.
    - Store helpers must support reroute continuity by reading the open blocker case for the current task, not just by source-task id.
    - Writes must stay atomic and YAML-backed, following the existing coordinator-store pattern.
  </behavior>
  <acceptance_criteria>
    - `rg -n "RunBlockersDir|RunBlockerCasePath" internal/mailbox/paths.go` returns matches.
    - `rg -n "CreateBlockerCase|ReadBlockerCase|UpdateBlockerCase|FindBlockerCaseByCurrentTaskID" internal/mailbox/coordinator_store.go` returns matches.
    - `rg -n "TestCoordinatorStoreBlockerCaseCRUD|TestCoordinatorStoreFindBlockerCaseByCurrentTaskID" internal/mailbox/coordinator_store_test.go` returns matches.
    - `go test ./internal/mailbox -run 'TestCoordinatorStoreBlockerCaseCRUD|TestCoordinatorStoreFindBlockerCaseByCurrentTaskID' -count=1` passes.
  </acceptance_criteria>
  <action>
Extend `internal/mailbox/paths.go` with exact helper names:
- `RunBlockersDir(stateDir string, runID protocol.RunID) string`
- `RunBlockerCasePath(stateDir string, runID protocol.RunID, sourceTaskID protocol.TaskID) string`

Extend `internal/mailbox/coordinator_store.go` with blocker-case persistence that mirrors the existing review-handoff API shape:
- `CreateBlockerCase(caseDoc *protocol.BlockerCase) error`
- `ReadBlockerCase(runID protocol.RunID, sourceTaskID protocol.TaskID) (*protocol.BlockerCase, error)`
- `UpdateBlockerCase(runID protocol.RunID, sourceTaskID protocol.TaskID, updateFn func(*protocol.BlockerCase) error) error`
- `FindBlockerCaseByCurrentTaskID(runID protocol.RunID, currentTaskID protocol.TaskID) (*protocol.BlockerCase, error)`

Implementation requirements:
- use YAML marshaling plus `writeFileAtomically`, just like `ReviewHandoff`
- validate the artifact before every create/update write
- treat multiple current-task matches as a hard failure
- ignore non-`.yaml` files when scanning `blockers/`

Add `internal/mailbox/coordinator_store_test.go` with direct persistence tests that create a blocker case, update it from `active` to `escalated`, and prove `FindBlockerCaseByCurrentTaskID` returns the same source-keyed artifact after `current_task_id` changes to a rerouted child task. Keep the source-task filename authoritative even when `current_task_id` moves.
  </action>
  <verify>
    <automated>go test ./internal/mailbox -run 'TestCoordinatorStoreBlockerCaseCRUD|TestCoordinatorStoreFindBlockerCaseByCurrentTaskID' -count=1</automated>
  </verify>
  <done>
The coordinator store can create, read, update, and scan blocker cases from the canonical `blockers/<source-task-id>.yaml` location, including reroute continuity lookup by current task id.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add blocker reroute ceiling config with dedicated defaults and loader tests</name>
  <files>
internal/config/config.go
internal/config/loader.go
internal/config/loader_test.go
  </files>
  <read_first>
.planning/phases/04-blocker-escalation/04-CONTEXT.md
.planning/phases/04-blocker-escalation/04-RESEARCH.md
internal/config/config.go
internal/config/loader.go
internal/config/loader_test.go
internal/runtime/daemon.go
  </read_first>
  <behavior>
    - Blocker reroute ceilings must live under `blockers.*` and stay separate from delivery retries per D-07 and D-11.
    - Per-task-class overrides are allowed, but only for valid canonical task classes.
    - The default ceiling should be conservative and explicit so reroute loops fail safe rather than hiding churn.
  </behavior>
  <acceptance_criteria>
    - `rg -n "type BlockersConfig struct|Blockers BlockersConfig" internal/config/config.go` returns matches.
    - `rg -n "max_reroutes_default|max_reroutes_by_task_class" internal/config/config.go internal/config/loader.go` returns matches.
    - `rg -n "TestLoadValidConfigWithBlockerRerouteCeilings|TestLoadRejectsInvalidBlockerRerouteCeilings" internal/config/loader_test.go` returns matches.
    - `go test ./internal/config -run 'TestLoadValidConfigWithBlockerRerouteCeilings|TestLoadRejectsInvalidBlockerRerouteCeilings' -count=1` passes.
  </acceptance_criteria>
  <action>
Add `BlockersConfig` to `internal/config/config.go` and wire it onto `Config` with this exact YAML surface:

```yaml
blockers:
  max_reroutes_default: 1
  max_reroutes_by_task_class:
    implementation: 1
    research: 1
    review: 0
```

Implementation requirements:
- `max_reroutes_default` defaults to `1` in `applyDefaults`
- `max_reroutes_default` and every override must be `>= 0`
- `max_reroutes_by_task_class` keys must validate through `protocol.TaskClass.Validate()`
- `clone()` must deep-copy the override map
- `delivery.max_retries` stays untouched and must not be referenced from blocker config validation

Add `internal/config/loader_test.go` coverage that proves:
- a valid config with blocker ceilings resolves successfully
- an invalid task-class key under `max_reroutes_by_task_class` fails
- a negative default or negative override fails with a stable `blockers.` error substring

Use the exact field names above so later plans can read `cfg.Blockers.MaxReroutesDefault` and `cfg.Blockers.MaxReroutesByTaskClass[taskClass]` without new config translation logic.
  </action>
  <verify>
    <automated>go test ./internal/config -run 'TestLoadValidConfigWithBlockerRerouteCeilings|TestLoadRejectsInvalidBlockerRerouteCeilings' -count=1</automated>
  </verify>
  <done>
The config layer exposes a dedicated blocker reroute budget with one explicit default and validated per-task-class overrides, separate from daemon transport retry settings.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/config input -> protocol/config validation | Untrusted `wait_kind`, `block_kind`, and reroute ceiling values cross into typed coordinator policy state here. |
| Filesystem artifact reads -> coordinator store | Blocker YAML can be stale, malformed, or tampered with before later session/rebuild code consumes it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | T | `internal/protocol/coordinator.go` / `internal/protocol/validation.go` | mitigate | Validate exact blocker enums, state-specific required fields, and source-task keyed identifiers before any write or update. |
| T-04-01-02 | D | `internal/config/loader.go` | mitigate | Add dedicated `blockers.max_reroutes_*` validation with `>= 0` bounds and keep it separate from `delivery.max_retries`. |
| T-04-01-03 | T | `internal/mailbox/coordinator_store.go` | mitigate | Persist blocker cases only at `coordinator/runs/<run-id>/blockers/<source-task-id>.yaml`, validate on every create/update, and reject multiple matches when scanning by current task id. |
</threat_model>

<verification>
- `go test ./internal/protocol -run 'TestBlockerCaseValidateRequiresStructuredKinds|TestBlockerCaseValidateRequiresRecommendedActionForEscalation|TestBlockerResolutionActionValidate' -count=1`
- `go test ./internal/mailbox -run 'TestCoordinatorStoreBlockerCaseCRUD|TestCoordinatorStoreFindBlockerCaseByCurrentTaskID' -count=1`
- `go test ./internal/config -run 'TestLoadValidConfigWithBlockerRerouteCeilings|TestLoadRejectsInvalidBlockerRerouteCeilings' -count=1`
</verification>

<success_criteria>
- `BlockerCase` is a validated protocol artifact with exact blocker/action/resolution enums and escalation requirements.
- Coordinator store persists blocker cases under `coordinator/runs/<run-id>/blockers/<source-task-id>.yaml` and supports current-task lookup for reroute continuity.
- Config exposes `blockers.max_reroutes_default` plus `blockers.max_reroutes_by_task_class` without touching daemon retry config.
</success_criteria>

<output>
After completion, create `.planning/phases/04-blocker-escalation/04-01-SUMMARY.md`
</output>
