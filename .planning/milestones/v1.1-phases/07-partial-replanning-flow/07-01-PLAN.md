---
phase: 07-partial-replanning-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/protocol/coordinator.go
  - internal/protocol/validation.go
  - internal/protocol/protocol_test.go
  - internal/mailbox/paths.go
  - internal/mailbox/coordinator_store.go
  - internal/mailbox/coordinator_store_test.go
autonomous: true
requirements:
  - REPLAN-01
  - REPLAN-02
must_haves:
  truths:
    - "A bounded partial replan is represented by one durable source-task keyed artifact, not runtime-only state, so the operator can reconstruct what was superseded and what replaced it after restart."
    - "Replacement lineage stays local to the blocked source task per D-01 and D-02: one source task, one blocker case, one superseded current task, and one replacement task."
    - "Partial replans are explicitly bounded per D-05 and D-06: they are a blocker-resolution action, they cannot recurse, and they do not create an unbounded mini-plan tree."
  artifacts:
    - path: internal/protocol/coordinator.go
      provides: "Canonical `PartialReplan` protocol contract plus the `partial_replan` blocker-resolution action"
    - path: internal/protocol/validation.go
      provides: "Fail-loud validation for replan lineage, bounded replacement semantics, and immutable artifact shape"
    - path: internal/mailbox/paths.go
      provides: "Canonical `coordinator/runs/<run-id>/replans/<source-task-id>.yaml` storage paths"
    - path: internal/mailbox/coordinator_store.go
      provides: "Create/read/lookup helpers for partial replans keyed by source task and replacement task"
    - path: internal/mailbox/coordinator_store_test.go
      provides: "Direct regression coverage for durable partial-replan persistence and lookup"
  key_links:
    - from: internal/protocol/coordinator.go
      to: internal/protocol/validation.go
      via: "The new `PartialReplan` contract and `partial_replan` resolution action must validate before session code can execute them"
      pattern: "PartialReplan|BlockerResolutionActionPartialReplan|Validate"
    - from: internal/mailbox/paths.go
      to: internal/mailbox/coordinator_store.go
      via: "Coordinator store reads and writes the canonical source-task keyed replan artifact path"
      pattern: "RunPartialReplansDir|RunPartialReplanPath|CreatePartialReplan"
    - from: internal/mailbox/coordinator_store.go
      to: internal/session/blocker_resolve.go
      via: "The later execution path must be able to reject duplicate replans and map replacement tasks back to their source lineage"
      pattern: "ReadPartialReplan|FindPartialReplanByReplacementTaskID"
---

<objective>
Define the durable partial-replan contract, replacement lineage semantics, and bounded recovery rules for Phase 7.

Purpose: Satisfy D-01 through D-06 before any CLI or session behavior changes by pinning one explicit, source-task-local replan artifact that extends blocker resolution without introducing autonomous recursive replanning.
Output: A validated `PartialReplan` artifact, canonical replan storage helpers under the coordinator tree, and regression coverage for single-replacement bounded semantics.
</objective>

<execution_context>
@/Users/chsong/.codex/get-shit-done/workflows/execute-plan.md
@/Users/chsong/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-partial-replanning-flow/07-CONTEXT.md
@.planning/milestones/v1.0-phases/04-blocker-escalation/04-01-SUMMARY.md
@.planning/milestones/v1.0-phases/04-blocker-escalation/04-02-SUMMARY.md
@.planning/milestones/v1.0-phases/04-blocker-escalation/04-03-SUMMARY.md
@.planning/milestones/v1.0-phases/03-review-handoff-flow/03-02-SUMMARY.md
@.planning/phases/06-adaptive-routing-signals/06-01-SUMMARY.md
@.planning/phases/06-adaptive-routing-signals/06-02-SUMMARY.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONCERNS.md
@internal/protocol/coordinator.go
@internal/protocol/validation.go
@internal/mailbox/paths.go
@internal/mailbox/coordinator_store.go

<interfaces>
Current blocker and task lineage seam:
```go
type BlockerCase struct {
	RunID            RunID
	SourceTaskID     TaskID
	SourceMessageID  MessageID
	CurrentTaskID    TaskID
	CurrentMessageID MessageID
	CurrentOwner     AgentName
	Status           BlockerStatus
	Resolution       *BlockerResolution
}

type BlockerResolution struct {
	Action           BlockerResolutionAction
	CreatedTaskID    TaskID
	CreatedMessageID MessageID
}
```

Current coordinator-store workflow pattern:
```go
func (s *CoordinatorStore) CreateBlockerCase(caseDoc *protocol.BlockerCase) error
func (s *CoordinatorStore) ReadBlockerCase(runID protocol.RunID, sourceTaskID protocol.TaskID) (*protocol.BlockerCase, error)
func (s *CoordinatorStore) FindBlockerCaseByCurrentTaskID(runID protocol.RunID, currentTaskID protocol.TaskID) (*protocol.BlockerCase, error)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add red tests for partial-replan validation, bounded replacement semantics, and coordinator-store lookup</name>
  <files>internal/protocol/protocol_test.go, internal/mailbox/coordinator_store_test.go</files>
  <read_first>
    .planning/phases/07-partial-replanning-flow/07-CONTEXT.md
    .planning/milestones/v1.0-phases/04-blocker-escalation/04-01-SUMMARY.md
    .planning/milestones/v1.0-phases/04-blocker-escalation/04-02-SUMMARY.md
    internal/protocol/coordinator.go
    internal/protocol/validation.go
    internal/protocol/protocol_test.go
    internal/mailbox/paths.go
    internal/mailbox/coordinator_store.go
    internal/mailbox/coordinator_store_test.go
  </read_first>
  <behavior>
    - Per D-01 and D-02, a partial replan must stay anchored to one source task and one blocker case, while still recording the superseded current task and the replacement task explicitly.
    - Per D-05 and D-06, partial replans are bounded: one artifact per source task, one superseded current task, and exactly one replacement task. Any nested or recursive lineage must be rejected at validation time.
    - The durable artifact must live under the coordinator tree and support lookup by replacement task so later run-inspection code can render both the source-task view and the replacement-task view from disk alone.
  </behavior>
  <action>
Add these exact failing tests.

In `internal/protocol/protocol_test.go`:
- `TestPartialReplanValidateRequiresSourceBlockerAndReplacementLineage`
- `TestPartialReplanValidateRejectsRecursiveOrDuplicateReplacementLinks`
- Extend the blocker-resolution action enum test so `partial_replan` is treated as a valid resolution action.

The protocol fixtures must assert these exact rules:
- `blocker_source_task_id` is required and must equal `source_task_id`
- `superseded_task_id` and `replacement_task_id` are both required generated `task_...` identifiers
- `replacement_task_id` must differ from both `source_task_id` and `superseded_task_id`
- `reason` is required
- `status` must be `applied`

In `internal/mailbox/coordinator_store_test.go`:
- `TestCoordinatorStorePartialReplanCRUDAndLookupByReplacementTask`

The store test must create one partial replan at the canonical path `coordinator/runs/<run-id>/replans/<source-task-id>.yaml`, read it back, and prove `FindPartialReplanByReplacementTaskID` resolves the same artifact from the replacement task id.
  </action>
  <acceptance_criteria>
    - `rg -n "TestPartialReplanValidateRequiresSourceBlockerAndReplacementLineage|TestPartialReplanValidateRejectsRecursiveOrDuplicateReplacementLinks|TestCoordinatorStorePartialReplanCRUDAndLookupByReplacementTask" internal/protocol/protocol_test.go internal/mailbox/coordinator_store_test.go` returns all three test names.
    - `internal/protocol/protocol_test.go` contains the exact field names `blocker_source_task_id`, `superseded_task_id`, `replacement_task_id`, and `status`.
    - `go test ./internal/protocol ./internal/mailbox -run 'TestPartialReplanValidateRequiresSourceBlockerAndReplacementLineage|TestPartialReplanValidateRejectsRecursiveOrDuplicateReplacementLinks|TestCoordinatorStorePartialReplanCRUDAndLookupByReplacementTask' -count=1` fails before implementation lands.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/protocol ./internal/mailbox -run 'TestPartialReplanValidateRequiresSourceBlockerAndReplacementLineage|TestPartialReplanValidateRejectsRecursiveOrDuplicateReplacementLinks|TestCoordinatorStorePartialReplanCRUDAndLookupByReplacementTask' -count=1</automated>
  </verify>
  <done>Red tests pin the single-replacement bounded replan contract, path shape, and replacement-task lookup before protocol or store code changes land.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the durable partial-replan contract, canonical storage paths, and lookup helpers</name>
  <files>internal/protocol/coordinator.go, internal/protocol/validation.go, internal/protocol/protocol_test.go, internal/mailbox/paths.go, internal/mailbox/coordinator_store.go, internal/mailbox/coordinator_store_test.go</files>
  <read_first>
    .planning/phases/07-partial-replanning-flow/07-CONTEXT.md
    .planning/milestones/v1.0-phases/04-blocker-escalation/04-01-SUMMARY.md
    .planning/milestones/v1.0-phases/04-blocker-escalation/04-02-SUMMARY.md
    .planning/milestones/v1.0-phases/04-blocker-escalation/04-03-SUMMARY.md
    internal/protocol/coordinator.go
    internal/protocol/validation.go
    internal/mailbox/paths.go
    internal/mailbox/coordinator_store.go
  </read_first>
  <action>
Implement the partial-replan contract with these exact boundaries.

Protocol surface, per D-01 through D-06:
- Extend `BlockerResolutionAction` with `BlockerResolutionActionPartialReplan = "partial_replan"`.
- Add `PartialReplanStatus` with the exact value `applied`.
- Add a canonical `PartialReplan` struct with these exact YAML fields:
  - `run_id`
  - `source_task_id`
  - `source_message_id`
  - `blocker_source_task_id`
  - `superseded_task_id`
  - `superseded_message_id`
  - `superseded_owner`
  - `replacement_task_id`
  - `replacement_message_id`
  - `replacement_owner`
  - `reason`
  - `status`
  - `created_at`
  - `updated_at`

Validation rules in `internal/protocol/validation.go`:
- `run_id`, `source_task_id`, `blocker_source_task_id`, `superseded_task_id`, and `replacement_task_id` must all use the existing generated identifiers.
- `source_message_id`, `superseded_message_id`, and `replacement_message_id` must all use generated `msg_...` identifiers.
- `blocker_source_task_id` must equal `source_task_id` so the replan stays local to the blocked source work per D-01 and D-02.
- `superseded_task_id` may equal `source_task_id` when the original source task itself was blocked, but `replacement_task_id` must differ from both.
- `superseded_owner`, `replacement_owner`, and `reason` must be non-blank.
- `status` must validate against the single exact value `applied`.

Coordinator-store support:
- In `internal/mailbox/paths.go`, add `RunPartialReplansDir(stateDir string, runID protocol.RunID) string` and `RunPartialReplanPath(stateDir string, runID protocol.RunID, sourceTaskID protocol.TaskID) string`.
- In `internal/mailbox/coordinator_store.go`, add:
  - `CreatePartialReplan(replan *protocol.PartialReplan) error`
  - `ReadPartialReplan(runID protocol.RunID, sourceTaskID protocol.TaskID) (*protocol.PartialReplan, error)`
  - `FindPartialReplanByReplacementTaskID(runID protocol.RunID, replacementTaskID protocol.TaskID) (*protocol.PartialReplan, error)`
- Keep the artifact immutable in this phase: create/read/find only. Do not add update semantics or second-generation lineage mutation.
- `FindPartialReplanByReplacementTaskID` must ignore non-`.yaml` files, hard-fail on multiple matches, and validate that the YAML `run_id` plus `source_task_id` still match the source-keyed path.

Do not add a new runtime daemon path, a replan-only command, or any remote/timeline behavior in this plan.
  </action>
  <acceptance_criteria>
    - `rg -n "BlockerResolutionActionPartialReplan|type PartialReplanStatus string|type PartialReplan struct" internal/protocol/coordinator.go` returns the new contract.
    - `rg -n "func \\(p \\*PartialReplan\\) Validate|blocker_source_task_id|replacement_task_id|status must" internal/protocol/validation.go` returns the new validation seam and bounded-lineage checks.
    - `rg -n "RunPartialReplansDir|RunPartialReplanPath|CreatePartialReplan|ReadPartialReplan|FindPartialReplanByReplacementTaskID" internal/mailbox/paths.go internal/mailbox/coordinator_store.go` returns all storage helpers.
    - `go test ./internal/protocol ./internal/mailbox -run 'TestPartialReplanValidateRequiresSourceBlockerAndReplacementLineage|TestPartialReplanValidateRejectsRecursiveOrDuplicateReplacementLinks|TestCoordinatorStorePartialReplanCRUDAndLookupByReplacementTask' -count=1` passes.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/protocol ./internal/mailbox -run 'TestPartialReplanValidateRequiresSourceBlockerAndReplacementLineage|TestPartialReplanValidateRejectsRecursiveOrDuplicateReplacementLinks|TestCoordinatorStorePartialReplanCRUDAndLookupByReplacementTask' -count=1 && go test ./internal/protocol ./internal/mailbox -count=1</automated>
  </verify>
  <done>The repo has a durable, validated, source-task keyed partial-replan artifact and canonical store helpers that later execution code can rely on without inventing runtime-only lineage state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Partial replan YAML -> later session/rebuild flows | Replan lineage becomes authoritative for source/superseded/replacement relationships. |
| Blocker resolution action enum -> operator/session behavior | The new `partial_replan` action must not silently broaden into recursive workflow control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | T | `internal/mailbox/coordinator_store.go` | mitigate | Validate `run_id` and `source_task_id` against the source-keyed path on every read and reject mismatches as hard failures. |
| T-07-01-02 | E | `internal/protocol/validation.go` | mitigate | Constrain the contract to one source task, one blocker case, one superseded task, and one replacement task so `partial_replan` cannot become recursive autonomy. |
| T-07-01-03 | R | `internal/protocol/coordinator.go` | mitigate | Keep the artifact durable and explicit under `coordinator/runs/<run-id>/replans/` so later run-show and blocker-resolution flows can reconstruct lineage from disk alone. |
</threat_model>

<verification>
Run `go test ./internal/protocol ./internal/mailbox -run 'TestPartialReplanValidateRequiresSourceBlockerAndReplacementLineage|TestPartialReplanValidateRejectsRecursiveOrDuplicateReplacementLinks|TestCoordinatorStorePartialReplanCRUDAndLookupByReplacementTask' -count=1` after Task 2, then run `go test ./internal/protocol ./internal/mailbox -count=1` before closing the plan.
</verification>

<success_criteria>
- The repo defines a durable `PartialReplan` artifact that keeps source-task, blocker, superseded-task, and replacement-task lineage explicit.
- The artifact is bounded and non-recursive by validation, with no runtime-only fallback state.
- Session execution code in the next plan can create or inspect replans through canonical store helpers instead of ad hoc YAML access.
</success_criteria>

<output>
After completion, create `.planning/phases/07-partial-replanning-flow/07-01-SUMMARY.md`
</output>
